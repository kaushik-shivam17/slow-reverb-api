[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.10"
dependencies = [
    "flask==3.0.3",
    "gunicorn==22.0.0",
    "pydub==0.25.1",
    "requests==2.32.3",
]
